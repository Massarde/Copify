/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
	/* config options here */
}
module.exports = {
	images: {
		domains: ['i.scdn.co', 'platform-lookaside.fbsbx.com'],
	},
}
// module.exports = nextConfig
